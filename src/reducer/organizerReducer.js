const DISPLAY_MODE = {
  MONTH: "MONTH",
  WEEK: "WEEK",
  DAY: "DAY",
};

const initialState = {
  displayMode: DISPLAY_MODE.MONTH,
};

// export const organizerReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case value:

//       break;

//     default:
//       break;
//   }
// }
